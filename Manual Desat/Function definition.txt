(   
     bool "debug"=false,
     float "desat"=0,
     float "min_chroma"=1 (target max relative difference in chroma after desaturation), 
     bool "neg_bias" =true (tend to subtract from hues rather than add to them in auto hue correction)
)
