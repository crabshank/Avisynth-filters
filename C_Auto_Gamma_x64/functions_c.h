#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))
#define lerp(a,b,t) ((1 - (t)) * (a) + (t) * (b) )
#include "C:\Program Files (x86)\~AvsPmod\plugins\C_White_Point_x64\C_White_Point\xyY_funcs.h"
